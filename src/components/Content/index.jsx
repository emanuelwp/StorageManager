import styled from "styled-components";

const Content = styled.div`
  margin-left: 250px; /* Ajuste para o mesmo valor da largura da sidebar */
  padding: 20px;
  padding-top: 80px; /* Espaço suficiente para o cabeçalho */
`;

export default Content;
